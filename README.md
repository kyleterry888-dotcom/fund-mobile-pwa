# 自用基金估值助手 Mobile PWA

版本：v1.0.16（普通模式也移除列表里的重仓覆盖率/行情数量，统一放入详情）

这是手机自用版。它是纯静态网页，不需要后端、不需要数据库、不需要 API Key。

## 怎么运行

本地测试：

```bash
cd fund_mobile_pwa
python -m http.server 8010
```

浏览器打开：

```text
http://localhost:8010
```

手机使用时，建议部署到 HTTPS 静态网页服务，例如 Cloudflare Pages、GitHub Pages、Vercel、Netlify 或自己的服务器。

## iPhone 添加到主屏幕

1. 用 Safari 打开你部署后的网址
2. 点底部“分享”按钮
3. 选择“添加到主屏幕”
4. 桌面会出现“基金估值”图标

## 安卓安装

用 Chrome 打开后，通常会出现“安装”或“添加到主屏幕”。也可以点页面里的“安装”。

## 数据保存在哪里

基金列表、份额、成本、备注、手动重仓都保存在当前浏览器的 localStorage。

换手机、换浏览器、清理 Safari 网站数据之前，请先用“导出备份”。

## 估值逻辑

- 平台估值：读取天天基金估值接口
- 最新净值/走势图：读取东方财富历史净值
- 自动重仓：读取东方财富 F10 前十大持仓
- 个股行情：读取腾讯行情
- Top10 贡献：Σ 重仓权重 × 个股涨跌
- Top10 归一化：Top10 贡献 / Top10 覆盖率
- 走势图：支持近1月/3月/6月/1年/全部区间，每次切换都会按真实日期重新过滤净值、重算涨跌/最大回撤，并局部重绘 SVG 图和最近净值明细

## 注意

公开接口可能会因为跨域、限流、接口变更而偶尔失败。这个工具适合自用估算，不适合作为真实交易依据。


## v1.0.16
- 普通模式列表不再展示“自动重仓/手动重仓 · 覆盖 xx% · x/x 有行情”。
- 重仓覆盖率、报告期、有行情数量统一放到基金详情页。
- 列表页更紧凑，普通模式只保留名称、操作、四项核心指标和净值状态徽标。

## v1.0.15

- 修复普通基金、港股/日经/韩股类基金被误标为“净值滞后/净值已更新”的问题。
- 列表页恢复为：净值日未覆盖当前估值日时，普通基金优先展示盘中估值。
- 仅美股/明显海外滞后类 QDII 才显示“海外净值已更新/海外净值滞后”。
- 估值拆分中的净值状态改为“估值优先/海外已更新/海外滞后”，避免把普通基金误判成 QDII 滞后。

## v1.0.12

- 走势图触摸/悬停提示框改为半透明。
- 提示框会根据触摸点位置自动放到左侧或右侧，避免挡住正在查看的曲线点。
- 保留十字线和圆点，让你能更清楚地看到当前定位位置。

## v1.0.11

- 拆分“净值日”和“发布时间”的判断。
- QDII/跨境基金如果 25 号公布 22 号净值，会主展示这条最新官方净值，并标记为“净值已更新/滞后”。
- 这类涨幅会显示为“净值日表现”，不会再误当成 25 号当天涨幅。
- 如果旧官方净值不是今天新检测到的，才继续作为滞后参考，并优先使用盘中估值。

## v1.0.10

- 新增“官方净值是否覆盖当前估值日”的判断。
- QDII/跨境基金如果 25 号只更新到 22 号净值，会标记为“净值滞后”，继续优先展示盘中估值。
- 港股等当天能更新到当前估值日的基金，仍会自动切换为官方净值。


## v1.0.9

- 走势图主图从“单位净值”改为“区间涨跌幅”，更适合对比近1月/3月/6月/1年走势。
- 图表区域放大，并增加触摸/鼠标悬停提示：日期、区间涨跌、当日涨幅、单位净值、净值差。
- 保留日涨跌柱、区间高低点、最大回撤和最近涨跌明细。

## v1.0.7
- 历史净值走势改为优先使用东方财富 pingzhongdata 长周期净值序列。
- F10 历史净值改为分页兜底，并用最近 F10 数据覆盖最新净值和日涨幅。
- 走势图下方增加数据源、缓存条数和历史覆盖不足提示。


## v1.0.9

- 将走势图最新点信息移到图表右侧标签区域，避免遮挡收益率曲线。
- 最新标签同时显示区间涨跌和最新单位净值。
